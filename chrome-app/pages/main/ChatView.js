define(function(require) {

    // Constructor
    var ChatView = function () {
    };

    var C = ChatView.prototype;

    /*
     * UI
     *
     */

    C.loadBacklog = function () {
    };

    return ChatView;

});
